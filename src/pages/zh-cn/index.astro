---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { getSiteDescription, getPlatforms, getResearchAreas, getResearchCenters, getFeaturedProjects, getFeaturedPublications, getUpcomingEvents } from '../../data/site';
import type { Lang } from '../../i18n/utils';

const lang = (Astro.currentLocale as Lang) ?? 'en';
const platforms = getPlatforms(lang);
const researchAreas = getResearchAreas(lang);
const researchCenters = getResearchCenters(lang);
const featuredProjects = getFeaturedProjects(lang);
const featuredPublications = getFeaturedPublications(lang);
const upcomingEvents = getUpcomingEvents(lang);

const homeFrameworkUnits = [
  { unit: "研究中心",              role: "专业学术单位" },
  { unit: "研究项目",             role: "研究院支持的项目" },
  { unit: "学术出版事务部",             role: "报告、论文与简报" },
  { unit: "学术活动",               role: "会议、研讨会、工作坊" },
  { unit: "学者计划",  role: "研究员、访问学者与研究助理" },
  { unit: "合作事务办公室",          role: "伙伴关系、提案与谅解备忘录" },
];
---

<BaseLayout
  title="全景研究院"
  description={getSiteDescription(lang)}
>

<!-- ── 02 Hero ── -->
<section class="hero-section" style="background:var(--bg-main);border-bottom:1px solid var(--border-light);">
  <div class="container">
    <div class="hero-inner">
      <!-- Left: text -->
      <div class="hero-text">
        <div style="font-family:var(--font-mono);font-size:12px;letter-spacing:0.14em;text-transform:uppercase;color:var(--text-muted);margin-bottom:28px;">
          全景学术集团内部的研究院
        </div>
        <h1 style="font-family:var(--font-serif);font-size:clamp(38px,5.5vw,60px);line-height:1.08;letter-spacing:-0.035em;color:var(--text-main);margin-bottom:28px;max-width:600px;">
          全景研究院
        </h1>
        <p style="font-size:18px;line-height:1.75;color:var(--text-secondary);max-width:560px;margin-bottom:36px;">
          研究、学术基础设施与学术合作。研究院支持跨学科研究、学术出版研究、期刊索引与评价、政策分析，以及全球学术传播基础设施的发展。
        </p>
        <div style="display:flex;flex-wrap:wrap;gap:12px;">
          <a href="/zh-cn/about"            class="btn btn-primary">了解研究院</a>
          <a href="/zh-cn/research-centers" class="btn btn-secondary">查看研究中心</a>
          <a href="/zh-cn/charter"          class="btn btn-ghost">阅读章程 →</a>
        </div>
      </div>

      <!-- Right: abstract research structure graphic -->
      <div class="hero-graphic" aria-hidden="true">
        <svg viewBox="0 0 480 400" fill="none" xmlns="http://www.w3.org/2000/svg" style="width:100%;max-width:480px;">
          <!-- Grid background -->
          <defs>
            <pattern id="grid" x="0" y="0" width="40" height="40" patternUnits="userSpaceOnUse">
              <path d="M 40 0 L 0 0 0 40" fill="none" stroke="#DADDDD" stroke-width="0.5"/>
            </pattern>
          </defs>
          <rect width="480" height="400" fill="url(#grid)" />

          <!-- Central node: PRI -->
          <rect x="170" y="160" width="140" height="80" rx="2" fill="white" stroke="#1F2326" stroke-width="1.5"/>
          <text x="240" y="195" text-anchor="middle" font-family="'IBM Plex Sans',sans-serif" font-size="11" font-weight="600" fill="#1F2326" letter-spacing="1">全景</text>
          <text x="240" y="212" text-anchor="middle" font-family="'IBM Plex Sans',sans-serif" font-size="10" fill="#5C6368" letter-spacing="0.5">研究院</text>

          <!-- Top node: Research Centers -->
          <rect x="166" y="40" width="148" height="56" rx="2" fill="white" stroke="#DADDDD" stroke-width="1"/>
          <text x="240" y="65" text-anchor="middle" font-family="'IBM Plex Sans',sans-serif" font-size="10" font-weight="600" fill="#1F2326">研究中心</text>
          <text x="240" y="82" text-anchor="middle" font-family="'IBM Plex Sans',sans-serif" font-size="9" fill="#8A9196">4 个运行中的研究中心</text>
          <line x1="240" y1="96" x2="240" y2="160" stroke="#DADDDD" stroke-width="1" stroke-dasharray="4 3"/>

          <!-- Bottom node: Publications -->
          <rect x="166" y="304" width="148" height="56" rx="2" fill="white" stroke="#DADDDD" stroke-width="1"/>
          <text x="240" y="329" text-anchor="middle" font-family="'IBM Plex Sans',sans-serif" font-size="10" font-weight="600" fill="#1F2326">出版成果</text>
          <text x="240" y="346" text-anchor="middle" font-family="'IBM Plex Sans',sans-serif" font-size="9" fill="#8A9196">报告 · 论文 · 简报</text>
          <line x1="240" y1="240" x2="240" y2="304" stroke="#DADDDD" stroke-width="1" stroke-dasharray="4 3"/>

          <!-- Left node: Projects -->
          <rect x="20" y="164" width="120" height="56" rx="2" fill="white" stroke="#DADDDD" stroke-width="1"/>
          <text x="80" y="189" text-anchor="middle" font-family="'IBM Plex Sans',sans-serif" font-size="10" font-weight="600" fill="#1F2326">项目</text>
          <text x="80" y="206" text-anchor="middle" font-family="'IBM Plex Sans',sans-serif" font-size="9" fill="#8A9196">5 个进行中的项目</text>
          <line x1="140" y1="192" x2="170" y2="200" stroke="#DADDDD" stroke-width="1" stroke-dasharray="4 3"/>

          <!-- Right node: Scholars -->
          <rect x="340" y="164" width="120" height="56" rx="2" fill="white" stroke="#DADDDD" stroke-width="1"/>
          <text x="400" y="189" text-anchor="middle" font-family="'IBM Plex Sans',sans-serif" font-size="10" font-weight="600" fill="#1F2326">学者</text>
          <text x="400" y="206" text-anchor="middle" font-family="'IBM Plex Sans',sans-serif" font-size="9" fill="#8A9196">研究员 · 访问学者</text>
          <line x1="310" y1="192" x2="340" y2="200" stroke="#DADDDD" stroke-width="1" stroke-dasharray="4 3"/>

          <!-- Top-left: Scholarly Indexing -->
          <rect x="20" y="40" width="128" height="52" rx="2" fill="#F7F7F5" stroke="#C5C9C9" stroke-width="1"/>
          <text x="84" y="63" text-anchor="middle" font-family="'IBM Plex Sans',sans-serif" font-size="9.5" font-weight="600" fill="#375A6B">学术索引</text>
          <text x="84" y="79" text-anchor="middle" font-family="'IBM Plex Sans',sans-serif" font-size="8.5" fill="#8A9196">POSI 项目</text>
          <line x1="148" y1="66" x2="170" y2="175" stroke="#E6E8E8" stroke-width="1"/>

          <!-- Top-right: Policy Research -->
          <rect x="332" y="40" width="128" height="52" rx="2" fill="#F7F7F5" stroke="#C5C9C9" stroke-width="1"/>
          <text x="396" y="63" text-anchor="middle" font-family="'IBM Plex Sans',sans-serif" font-size="9.5" font-weight="600" fill="#375A6B">政策研究</text>
          <text x="396" y="79" text-anchor="middle" font-family="'IBM Plex Sans',sans-serif" font-size="8.5" fill="#8A9196">社会 · 治理</text>
          <line x1="332" y1="66" x2="310" y2="175" stroke="#E6E8E8" stroke-width="1"/>

          <!-- Bottom-left: Academic Events -->
          <rect x="20" y="308" width="128" height="52" rx="2" fill="#F7F7F5" stroke="#C5C9C9" stroke-width="1"/>
          <text x="84" y="331" text-anchor="middle" font-family="'IBM Plex Sans',sans-serif" font-size="9.5" font-weight="600" fill="#375A6B">学术活动</text>
          <text x="84" y="347" text-anchor="middle" font-family="'IBM Plex Sans',sans-serif" font-size="8.5" fill="#8A9196">会议 · 研讨会</text>
          <line x1="148" y1="334" x2="170" y2="240" stroke="#E6E8E8" stroke-width="1"/>

          <!-- Bottom-right: Collaboration -->
          <rect x="332" y="308" width="128" height="52" rx="2" fill="#F7F7F5" stroke="#C5C9C9" stroke-width="1"/>
          <text x="396" y="331" text-anchor="middle" font-family="'IBM Plex Sans',sans-serif" font-size="9.5" font-weight="600" fill="#375A6B">学术合作</text>
          <text x="396" y="347" text-anchor="middle" font-family="'IBM Plex Sans',sans-serif" font-size="8.5" fill="#8A9196">机构 · 伙伴</text>
          <line x1="332" y1="334" x2="310" y2="240" stroke="#E6E8E8" stroke-width="1"/>
        </svg>
      </div>
    </div>
  </div>
</section>

<!-- ── 03 Institutional Statement ── -->
<section class="section-compact" style="background:var(--bg-white);">
  <div class="container">
    <div style="max-width:880px;">
      <div class="statement-box">
        <div style="font-family:var(--font-mono);font-size:11px;letter-spacing:0.12em;text-transform:uppercase;color:var(--text-muted);margin-bottom:14px;">
          机构性质
        </div>
        <h2 style="font-family:var(--font-serif);font-size:22px;color:var(--text-main);margin-bottom:14px;font-weight:400;">
          全景学术集团内部的研究院
        </h2>
        <p style="font-size:15.5px;line-height:1.7;color:var(--text-secondary);margin-bottom:10px;">
          全景研究院是 Panorama Scholarly Group 的内部研究与学术发展部门，在研究活动中保持学术独立，但不具备独立法人资格。
        </p>
      </div>
    </div>
  </div>
</section>

<!-- ── 04 Institute Framework Preview ── -->
<section class="section" style="background:var(--bg-main);">
  <div class="container">
    <div style="margin-bottom:56px;">
      <h2 class="section-title">研究院组织与职能框架</h2>
      <p class="section-desc">
        一个结构化的研究与学术发展平台，为研究项目、学术出版、研究中心、学术活动、学者计划与机构合作提供支撑。
      </p>
    </div>

    <div class="org-structure-card" style="margin-bottom:48px;">
      <div class="org-hierarchy">
        <div class="org-node org-node-parent">
          全景学术集团
        </div>
        <div class="org-connector"></div>
        <div class="org-node org-node-institute">
          全景研究院
        </div>
        <div class="org-branch"></div>
        <div class="org-unit-grid">
          {homeFrameworkUnits.map((item, index) => (
            <div class="org-unit-card">
              <div class="org-unit-index">{String(index + 1).padStart(2, '0')}</div>
              <div>
                <div class="org-unit-title">{item.unit}</div>
                <div class="org-unit-role">{item.role}</div>
              </div>
            </div>
          ))}
        </div>
      </div>
    </div>

    <a href="/zh-cn/framework" class="btn btn-secondary">查看完整框架</a>
  </div>
</section>

<!-- ── 05 Research Areas ── -->
<section class="section" style="background:var(--bg-white);border-top:1px solid var(--border-light);">
  <div class="container">
    <div style="margin-bottom:56px;">
      <h2 class="section-title">研究领域</h2>
      <p class="section-desc">
        研究院聚焦连接学术传播、期刊评价、公共政策、教育、技术和文化研究的研究领域。
      </p>
    </div>
    <div class="areas-grid">
      {researchAreas.map((area) => (
        <div class="area-card">
          <div class="area-num">{area.num}</div>
          <h3 class="area-title">{area.title}</h3>
          <p class="area-text">{area.desc}</p>
        </div>
      ))}
    </div>
    <div style="margin-top:40px;">
      <a href="/zh-cn/research-areas" class="btn btn-secondary">查看全部研究领域</a>
    </div>
  </div>
</section>

<!-- ── 06 Research Centers ── -->
<section class="section" style="background:var(--bg-main);border-top:1px solid var(--border-light);">
  <div class="container">
    <div style="margin-bottom:48px;">
      <h2 class="section-title">研究中心</h2>
      <p class="section-desc">
        研究院通过专业研究中心组织学术活动，支持研究项目、出版成果、学术活动和合作关系。
      </p>
    </div>
    <div>
      {researchCenters.map((center) => (
        <div class="center-card">
          <div class="center-num">{center.num}</div>
          <div>
            <h3 style="font-family:var(--font-serif);font-size:20px;color:var(--text-main);margin-bottom:6px;">{center.name}</h3>
            <div style="display:flex;flex-wrap:wrap;gap:6px;">
              {center.areas.map((a) => <span class="tag">{a}</span>)}
              {center.upcoming && <span class="tag tag-steel">即将开展</span>}
            </div>
          </div>
          <a href={center.href} class="btn btn-ghost" style="padding-top:0;">查看中心 →</a>
        </div>
      ))}
    </div>
    <div style="margin-top:40px;">
      <a href="/zh-cn/research-centers" class="btn btn-secondary">全部研究中心</a>
    </div>
  </div>
</section>

<!-- ── 07 Featured Projects ── -->
<section class="section" style="background:var(--bg-white);border-top:1px solid var(--border-light);">
  <div class="container">
    <div style="margin-bottom:56px;">
      <h2 class="section-title">重点项目</h2>
      <p class="section-desc">
        全景研究院开发或支持的当前研究项目。
      </p>
    </div>
    <div class="projects-grid">
      {featuredProjects.map((p) => (
        <div class="project-card">
          <div style="margin-bottom:16px;">
            <span class={`tag ${p.status === '进行中' ? 'tag-active' : p.status === '参与征集' ? 'tag-steel' : ''}`}>
              {p.status}
            </span>
          </div>
          <h3 class="project-title">{p.title}</h3>
          <p class="project-desc">{p.desc}</p>
          <a href={p.href} style="display:inline-flex;align-items:center;gap:6px;margin-top:20px;font-size:13.5px;font-weight:500;color:var(--accent-steel);font-family:var(--font-sans);">
            查看项目 →
          </a>
        </div>
      ))}
    </div>
    <div style="margin-top:40px;">
      <a href="/zh-cn/projects" class="btn btn-secondary">全部研究项目</a>
    </div>
  </div>
</section>

<!-- ── 08 Publications ── -->
<section class="section" style="background:var(--bg-main);border-top:1px solid var(--border-light);">
  <div class="container">
    <div style="display:flex;justify-content:space-between;align-items:flex-end;margin-bottom:48px;gap:24px;">
      <div>
        <h2 class="section-title" style="margin-bottom:12px;">出版成果</h2>
        <p class="section-desc">
          研究院撰写或支持的研究报告、工作论文、政策简报、主编文集和年度报告。
        </p>
      </div>
      <a href="/zh-cn/publications" class="btn btn-secondary" style="flex-shrink:0;">全部出版成果</a>
    </div>

    <div>
      {featuredPublications.map((pub) => (
        <div class="publication-item">
          <div class="pub-type">{pub.type}</div>
          <div>
            <div class="pub-title">{pub.title}</div>
            <div class="pub-meta">{pub.team}</div>
          </div>
          <div style="text-align:right;">
            <div style="font-family:var(--font-mono);font-size:13px;color:var(--text-muted);margin-bottom:12px;">{pub.year}</div>
            <a href="/zh-cn/publications" class="btn btn-ghost" style="font-size:13px;">Details &rarr;</a>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>

<!-- ── 09 Charter & Governance Preview ── -->
<section class="section" style="background:var(--bg-white);border-top:1px solid var(--border-light);">
  <div class="container">
    <div style="display:grid;grid-template-columns:1fr 1fr;gap:72px;align-items:start;">
      <div>
        <h2 class="section-title">章程与治理</h2>
        <p class="section-desc" style="margin-bottom:32px;">
          研究院通过结构化的内部章程、治理规则、研究伦理原则、出版政策和合作程序运行。
        </p>
        <div style="display:flex;flex-wrap:wrap;gap:10px;">
          <a href="/zh-cn/charter"                              class="btn btn-primary">阅读章程</a>
          <a href="/zh-cn/governance/research-ethics"           class="btn btn-secondary">查看治理政策</a>
        </div>
      </div>
      <div>
        {[
          { label: "研究院章程",              href: "/zh-cn/charter" },
          { label: "治理规则",               href: "/zh-cn/governance/rules" },
          { label: "研究伦理",                href: "/zh-cn/governance/research-ethics" },
          { label: "学术独立声明",href: "/zh-cn/governance/academic-independence" },
          { label: "利益冲突政策",    href: "/zh-cn/governance/conflict-of-interest" },
          { label: "出版与报告政策",    href: "/zh-cn/governance/publication-report-policy" },
          { label: "数据与文档政策",    href: "/zh-cn/governance/data-documentation-policy" },
        ].map((item) => (
          <a href={item.href} style="display:flex;justify-content:space-between;align-items:center;padding:14px 0;border-bottom:1px solid var(--border-light);font-size:14.5px;color:var(--text-main);transition:color 0.15s;font-family:var(--font-sans);">
            {item.label}
            <span style="color:var(--text-muted);font-size:13px;">&rarr;</span>
          </a>
        ))}
      </div>
    </div>
  </div>
</section>

<!-- ── 10 Events & Calls ── -->
<section class="section" style="background:var(--bg-main);border-top:1px solid var(--border-light);">
  <div class="container">
    <div style="display:flex;justify-content:space-between;align-items:flex-end;margin-bottom:48px;gap:24px;">
      <div>
        <h2 class="section-title" style="margin-bottom:12px;">活动与征集</h2>
        <p class="section-desc">即将开展的会议、研讨会、工作坊、论文征稿和章节征集。</p>
      </div>
      <a href="/zh-cn/events" class="btn btn-secondary" style="flex-shrink:0;">全部活动</a>
    </div>
    <div>
      {upcomingEvents.map((ev) => (
        <div class="event-item">
          <div class="event-date">{ev.date.split(' ')[0]}<br />{ev.date.split(' ')[1]}</div>
          <div>
            <span class="tag" style="margin-bottom:10px;display:inline-flex;">{ev.type}</span>
            <h3 class="event-title">{ev.title}</h3>
            <p class="event-meta">{ev.location}</p>
          </div>
          <div style="text-align:right;padding-top:4px;">
            <span class="tag tag-steel">{ev.status}</span>
          </div>
        </div>
      ))}
    </div>
    <div style="margin-top:32px;display:flex;gap:12px;flex-wrap:wrap;">
      <a href="/zh-cn/events/calls-for-papers"   class="btn btn-secondary">论文征稿</a>
      <a href="/zh-cn/events/calls-for-chapters" class="btn btn-secondary">章节征集</a>
    </div>
  </div>
</section>

<!-- ── 11 Collaboration ── -->
<section class="section" style="background:var(--bg-dark);border-top:1px solid var(--border-light);">
  <div class="container">
    <div style="display:grid;grid-template-columns:1fr 1fr;gap:72px;align-items:center;">
      <div>
        <h2 style="font-family:var(--font-serif);font-size:clamp(26px,3.5vw,38px);line-height:1.2;color:var(--text-inverse);margin-bottom:20px;letter-spacing:-0.02em;">
          与全景研究院合作
        </h2>
        <p style="font-size:16px;line-height:1.75;color:rgba(255,255,255,0.6);margin-bottom:32px;">
          研究院欢迎学术合作、机构伙伴关系、研究项目提案、主编文集提案、访问学者申请和学术活动合作。
        </p>
        <div style="display:flex;flex-wrap:wrap;gap:10px;">
          <a href="/zh-cn/collaboration/submit" style="background:var(--bg-white);color:var(--text-main);border:1px solid var(--bg-white);" class="btn">提交提案</a>
          <a href="/zh-cn/contact"             style="background:transparent;color:var(--text-inverse);border:1px solid rgba(255,255,255,0.3);" class="btn">联系研究院</a>
        </div>
      </div>
      <div>
        {[
          "机构合作",
          "研究项目提案",
          "主编文集提案",
          "特刊提案",
          "访问学者申请",
          "研究员申请",
          "学术活动合作",
        ].map((type) => (
          <div style="display:flex;align-items:center;gap:12px;padding:12px 0;border-bottom:1px solid rgba(255,255,255,0.08);">
            <span style="width:5px;height:5px;background:rgba(255,255,255,0.3);border-radius:50%;flex-shrink:0;"></span>
            <span style="font-size:14.5px;color:rgba(255,255,255,0.7);font-family:var(--font-sans);">{type}</span>
          </div>
        ))}
      </div>
    </div>
  </div>
</section>

<!-- ── 12 Panorama Scholarly Ecosystem ── -->
<section class="section-compact" style="background:var(--bg-white);border-top:1px solid var(--border-light);">
  <div class="container">
    <div style="margin-bottom:40px;">
      <h2 class="section-title">全景学术生态体系组成部分</h2>
    </div>
    <div class="platform-grid platform-grid-refined">
      {platforms.map((p) => (
        <a href={p.href} target="_blank" rel="noopener noreferrer" class="platform-card">
          <div class={`platform-logo-box ${p.logoTheme === 'dark' ? 'platform-logo-box-dark' : ''}`}>
            <img src={p.logo} alt={`${p.name} logo`} loading="lazy" class="platform-logo" />
            <span class="platform-logo-fallback">{p.name}</span>
          </div>
          <div class="platform-card-body">
            <div>
              <div class="platform-card-name">{p.name}</div>
              <div class="platform-card-desc">{p.desc}</div>
            </div>
            <div class="platform-card-action">访问 <span aria-hidden="true">&rarr;</span></div>
          </div>
        </a>
      ))}
    </div>
  </div>
</section>

</BaseLayout>

<style>
.hero-section {
  padding: 100px 0 96px;
}
.hero-inner {
  display: grid;
  grid-template-columns: 1.1fr 0.9fr;
  gap: 72px;
  align-items: center;
}
.hero-graphic {
  display: flex;
  justify-content: center;
  align-items: center;
}
.areas-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 1px;
  background: var(--border-light);
  border: 1px solid var(--border-light);
}
.areas-grid .area-card {
  border: none;
  border-radius: 0;
}
.areas-grid .area-card:hover {
  transform: none;
  background: var(--bg-soft);
}
.projects-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 1px;
  background: var(--border-light);
  border: 1px solid var(--border-light);
}
.projects-grid .project-card {
  border: none;
  border-radius: 0;
}
.projects-grid .project-card:hover {
  transform: none;
  background: var(--bg-soft);
}
.platform-logo-box {
  min-height: 116px;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 8px;
  padding: 22px 20px;
  margin-bottom: 22px;
  border: 1px solid rgba(31, 35, 38, 0.10);
  border-radius: 8px;
  background:
    linear-gradient(180deg, rgba(255,255,255,0.96), rgba(247,247,245,0.92));
}
.platform-logo-box-dark {
  background:
    radial-gradient(circle at 20% 0%, rgba(255,255,255,0.10), transparent 32%),
    linear-gradient(135deg, #252A2E, #15191C);
  border-color: rgba(31, 35, 38, 0.28);
}
.platform-logo {
  width: auto;
  max-width: 92%;
  max-height: 44px;
  object-fit: contain;
}
.platform-logo-fallback {
  max-width: 100%;
  color: var(--text-secondary);
  font-family: var(--font-mono);
  font-size: 10px;
  font-weight: 600;
  letter-spacing: 0.08em;
  line-height: 1.25;
  text-align: center;
  text-transform: uppercase;
}
.platform-logo-box-dark .platform-logo-fallback {
  color: rgba(255,255,255,0.72);
}
.platform-card-body {
  min-height: 132px;
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  gap: 18px;
}
.platform-card-action {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  width: fit-content;
  font-size: 12.5px;
  font-weight: 600;
  color: var(--accent-steel);
  border-bottom: 1px solid rgba(55, 90, 107, 0.28);
  line-height: 1.35;
}

@media (max-width: 900px) {
  .hero-inner { grid-template-columns: 1fr; gap: 48px; }
  .hero-graphic { display: none; }
  .hero-section { padding: 64px 0; }
  .areas-grid { grid-template-columns: repeat(2, 1fr); }
  .projects-grid { grid-template-columns: 1fr; }
  .platform-logo-box {
    min-height: 108px;
    padding: 20px 18px;
  }
  .platform-logo {
    max-height: 40px;
  }
}

@media (max-width: 768px) {
  .areas-grid { grid-template-columns: 1fr; }
  section[style*="grid-template-columns:1fr 1fr"] > div > div {
    grid-template-columns: 1fr !important;
  }
  .platform-logo-box {
    min-height: 104px;
    margin-bottom: 18px;
  }
}
</style>
